# سياسة الخصوصية — USB Media Explorer

> آخر تحديث: 2026-09-09. هذه السياسة جزء من المستودع (`PRIVACY.md`) وهي المرجع
> المعتمد لأي نشر (GitHub Releases أو Google Play).

## الخلاصة في سطر واحد

**التطبيق يعمل دون اتصال بالكامل: لا يجمع أي بيانات، ولا يرسل أي شيء خارج
جهازك — لا تحليلات، ولا إعلانات، ولا تتبع، ولا نسخ احتياطي سحابي.**

## البيانات التي يخزنها التطبيق (على جهازك فقط)

| البيانات | أين تُحفظ | الغرض |
|---|---|---|
| المفضلة والملفات الأخيرة ومواضع التشغيل وتفضيلات المجلدات | ملفات JSON في التخزين الداخلي الخاص (`filesDir`) | عمل التطبيق نفسه |
| مصغرات الصور/الفيديو (كاش) | مجلد الكاش الخاص (`cacheDir/thumbs`) | سرعة العرض |
| تقرير التعطل (stack trace) | ملف خاص (`filesDir/crash_report.txt`) | عرضه لك عند التعطل |

- كل هذه البيانات تبقى على الجهاز ولا تُرفع إلى أي خادم — التطبيق **لا يطلب
  تصريح `INTERNET` أصلًا** ولا يحتوي أي مكتبة شبكية.
- النسخ الاحتياطي معطّل (`allowBackup=false`) وسجلّ التطبيق مستثنى صراحةً من
  قواعد النسخ (`backup_rules.xml` / `data_extraction_rules.xml`)، فلا يغادر
  سجلّك الجهاز حتى عبر نسخ النظام الاحتياطية.
- حذف بيانات التطبيق أو إزالة تثبيته يحذف كل ما سبق نهائيًا.

## تقرير التعطل

عند حدوث عطل يعرض التطبيق شاشة تقرير في عملية منفصلة. التقرير **لا يُرسل
تلقائيًا أبدًا** — أنت وحدك تقرر نسخه أو مشاركته (زرّا «نسخ»/«مشاركة»)،
كأي نص تشاركه يدويًا مع المطور.

## الصلاحيات ولماذا تُطلب

| الصلاحية | السبب |
|---|---|
| قراءة الوسائط (`READ_MEDIA_VIDEO/IMAGES/AUDIO` + الوصول الجزئي في Android 14) | تصفح ملفاتك الداخلية |
| `READ/WRITE_EXTERNAL_STORAGE` (للأجهزة القديمة فقط، بسقف SDK) | تصفح التخزين في Android 10 وما قبله |
| منح SAF (اختيار مجلد USB يدويًا) | الوصول إلى فلاشة USB — لا يحدث إلا باختيارك الصريح |
| خدمة أمامية + إشعارات + `WAKE_LOCK` | إكمال النسخ/النقل/الضغط في الخلفية مع شريط تقدم |

التطبيق **لا يطلب** `MANAGE_EXTERNAL_STORAGE` ولا الموقع ولا جهات الاتصال
ولا الكاميرا ولا الميكروفون.

## المشاركة مع تطبيقات أخرى

عند «مشاركة» أو «فتح بواسطة» ملف، يحصل التطبيق المستقبِل على **منح قراءة
لمرة واحدة لذلك الملف فقط** (`FLAG_GRANT_READ_URI_PERMISSION`) — لا يحصل
على أي وصول دائم أو شامل.

## الأطفال

التطبيق أداة ملفات عامة لا تستهدف الأطفال ولا تجمع بيانات من أي مستخدم.

## تغييرات السياسة

أي تغيير جوهري سيُوثّق هنا مع تحديث التاريخ أعلاه قبل النشر.

## التواصل

عبر [Issues المستودع](https://github.com/hashmllalan-cpu/USB2K/issues).
